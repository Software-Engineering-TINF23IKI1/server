@startuml
title BBC Server - Use Case Diagram
left to right direction

actor Client

rectangle "Game Server" {
    (Connect to Server)
    (Create Game Session)
    (Join Game Session)
    (Update Ready Status)
    (Send Click)
    (Shop Purchase Request)
    (Broadcast Game State)

    (Create Game Session) .> (Join Game Session) : <<include>>
}

Client --> (Connect to Server)
Client --> (Create Game Session)
Client --> (Join Game Session)
Client --> (Update Ready Status)
Client --> (Send Click)
Client --> (Shop Purchase Request)

' Representing internal periodic trigger
note right of (Broadcast Game State)
  Periodically triggered
  by game loop timer
end note

@enduml